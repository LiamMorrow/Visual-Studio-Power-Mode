<UserControl x:Class="PowerMode.ExplosionToolWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:powerMode="clr-namespace:PowerMode"
             Background="{DynamicResource VsBrush.Window}"
             Foreground="{DynamicResource VsBrush.WindowText}"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             Name="ExplosionToolWindow">
    <UserControl.Resources>
        <powerMode:ExplosionParticle x:Key="ExplosionParticle" />
    </UserControl.Resources>
    <xctk:PropertyGrid SelectedObject="{Binding Mode=OneWay, Source={StaticResource ExplosionParticle}}">
    </xctk:PropertyGrid>
</UserControl>